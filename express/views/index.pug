extends layout

block content
    h1 休み希望を入力するカレンダー
    hr
    #calendarform
    script(src="./dist/calendarform.js")
    each date in sleepdate
      script= `store.onCheckDate('${date.id}', '${date.date}')`
