extends ./../layout.pug

block content
  if main
    h1 main
    div.table-responsive
      table.table.table-striped
        each val, index in main
          tr
            th= index
            td= val
